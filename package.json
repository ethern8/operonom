{
  "name": "operonom",
  "private": true,
  "scripts": {
    "build": "lerna run --stream build",
    "bootstrap": "lerna bootstrap",
    "release": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
